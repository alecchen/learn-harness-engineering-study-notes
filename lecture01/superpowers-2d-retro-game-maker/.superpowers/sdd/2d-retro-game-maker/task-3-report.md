# Task 3: Sprite Editor mode

**Status:** DONE

**Commits:**
- `feat: add sprite editor mode`

**Test summary:**
- Open `index.html` in browser, click "Sprite Editor" tab
- Sprite editor shows 16x32 pixel grid with red border and label
- Color palette swatches render in toolbar, click to select
- Painting on grid with left-click writes selected color
- Right-click erase writes 255 (transparent)
- Preview thumbnail (32x64 canvas, 64x128 CSS) updates in real-time
- Clear button fills all pixels with 255 (transparent)
- Fill button fills all pixels with selected color
- Switching modes preserves sprite data

**Concerns:** None.
